{% if Base.UseLanguageExt -%}
using LanguageExt.Common;

{% endif -%}
namespace {{ Type.Namespace }};

{% if Base.UseLanguageExt -%}
public class {{ Type.Name }}() : IRequestHandler<{{ Base.Command.Type.Name }}, Result<{{ Base.ReturnType }}>>
{
    public async Task<Result<{{ Base.ReturnType }}>> Handle({{ Base.Command.Type.Name }} request, CancellationToken cancellationToken)
    {
        throw new NotImplementedException();
    }
}
{% else -%}
public class {{ Type.Name }}() : IRequestHandler<{{ Base.Command.Type.Name }}, {{ Base.ReturnType }}>
{
    public async Task<{{ Base.ReturnType }}> Handle({{ Base.Command.Type.Name }} request, CancellationToken cancellationToken)
    {
        throw new NotImplementedException();
    }
}
{% endif -%}
